// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// Astro natively supports PUBLIC_ environment variables
// Set PUBLIC_BACKEND_URL at build time to configure the backend API URL
export default defineConfig({});
